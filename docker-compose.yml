version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beatflow-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ./logs:/app/logs
